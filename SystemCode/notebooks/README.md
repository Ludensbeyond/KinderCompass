# Notebooks

Jupyter notebooks for exploration, prototyping, and demos.

## Planned contents

| Notebook | Purpose |
|----------|---------|
| Data exploration | Inspect raw CSVs and GeoJSON from `data/raw/` |
| Graph prototyping | Test knowledge graph construction before moving to `src/scripts/` |
| Engine demos | Prototype each reasoning engine before API integration |
| Workshop notes | Course module experiments (MR, RS, CGS) relevant to the project |

## Rules

- Use notebooks for **exploration and demonstration**.
- Move stable, reusable logic into `src/scripts/` or `src/backend/` when ready.
